import { ScreenerNameSearchPipe } from './screener-name-search.pipe';

describe('ScreenerNameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ScreenerNameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
