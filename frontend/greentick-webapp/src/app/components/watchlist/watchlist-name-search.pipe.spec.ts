import { WatchlistNameSearchPipe } from './watchlist-name-search.pipe';

describe('WatchlistNameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new WatchlistNameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
