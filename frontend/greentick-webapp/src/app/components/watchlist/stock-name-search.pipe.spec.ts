import { StockNameSearchPipe } from './stock-name-search.pipe';

describe('StockNameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StockNameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
