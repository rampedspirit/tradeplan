import { FilterNameSearchPipe } from './filter-name-search.pipe';

describe('FilterNameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
